# @intexuraos/infra-pubsub

Cloud Pub/Sub abstract `BasePubSubPublisher` and concrete publisher helpers.

## Contract

- **Layer:** infra-wrapper
- **Dependencies:** `@intexuraos/common-core`
- **Exports:** `./src/index.ts` (source-exports — no `dist/` emission)

## Usage

```ts
import { BasePubSubPublisher } from '@intexuraos/infra-pubsub';
```

For full API documentation, see [`docs/packages/infra-pubsub/README.md`](../../docs/packages/infra-pubsub/README.md).

## Tests

```bash
pnpm vitest run packages/infra-pubsub
```
