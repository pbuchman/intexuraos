/**
 * Port for embedding repository operations.
 * Re-exports from domain/models for consistency.
 */
export type {
  DocChunk,
  DocChunkWithScore,
  EmbeddingRepositoryPort,
} from '../models/docChunk.js';
