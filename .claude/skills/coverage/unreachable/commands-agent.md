# Coverage Exemptions: commands-agent

> Auto-generated by `/coverage`. Manual edits preserved if format maintained.

## Summary

| Metric | Count |
|--------|-------|
| Total exempted branches | 1 |
| Last verified | 2026-01-30 |

---

## `src/routes/internalRoutes.ts`

### Line ~21: authentication guard

```typescript
if (user === null) {
  return reply.fail(401, 'Unauthorized');
}
```

- **Blocker:** Test Infrastructure Constraints
- **Proof:** The test infrastructure uses `FakeAuthPlugin` which always returns a valid user object. It doesn't have a method to simulate authentication failures. The null branch requires an unauthenticated request, which the test fake cannot produce. Authentication failures are tested at the middleware level separately.

---

## Verification Date: 2026-01-30

## Auditor: Claude Code
