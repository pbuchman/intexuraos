# Coverage Exemptions: data-insights-agent

> Auto-generated by `/coverage`. Manual edits preserved if format maintained.

## Summary

| Metric | Count |
|--------|-------|
| Total exempted branches | 2 |
| Last verified | 2026-01-30 |

---

## `src/routes/dataInsightsRoutes.ts`

### Line ~25: authentication guard

```typescript
if (user === null) {
  return reply.fail(401, 'Unauthorized');
}
```

- **Blocker:** Test Infrastructure Constraints
- **Proof:** The test infrastructure uses `FakeAuthPlugin` which always returns a valid user object. It doesn't have a method to simulate authentication failures. The null branch requires an unauthenticated request, which the test fake cannot produce.

### Line ~27: admin guard

```typescript
if (user.role !== 'admin') {
  return reply.fail(403, 'Forbidden');
}
```

- **Blocker:** Test Infrastructure Constraints
- **Proof:** The test user in `FakeAuthPlugin` has a hardcoded role of 'user'. The test infrastructure doesn't provide a way to simulate users with different roles. The admin role check requires a user with 'admin' role, which the test fake cannot produce.

---

## Verification Date: 2026-01-30

## Auditor: Claude Code
